[
  {
    "id": "calendar-recovery",
    "claim": "Keeps local calendar copies ready to compare and restore",
    "where": "landing headline and README",
    "test": "npm run test:e2e -- --grep @claim:calendar-recovery",
    "sandbox": "fresh browser archive; import two changed ICS copies, select a deleted event, and download its restore file"
  },
  {
    "id": "reject-truncated-ics",
    "claim": "Rejects incomplete calendar files before changing the archive",
    "where": "calendar file import error",
    "test": "npm run test:e2e -- --grep @claim:reject-truncated-ics",
    "sandbox": "shipped two-copy demo archive; import an unclosed VCALENDAR and VEVENT, assert the error and unchanged edition count, then import a complete file"
  },
  {
    "id": "sample-project",
    "claim": "Try the app with an isolated sample calendar project",
    "where": "landing first screen, demo page, desktop first-run screen",
    "test": "npm run test:e2e -- --grep @claim:sample-project",
    "sandbox": "fresh browser context at /demo/; assert two sample editions and the persistent demo banner"
  },
  {
    "id": "calendar-diff",
    "claim": "Shows moved and cancelled events between calendar copies",
    "where": "landing How it works and desktop sample project",
    "test": "npm run test:e2e -- --grep @claim:calendar-diff",
    "sandbox": "fresh browser context at /demo/; inspect the shipped sample edition"
  },
  {
    "id": "ics-restore-export",
    "claim": "Exports selected earlier events as a standard ICS restore file",
    "where": "landing How it works, desktop sample project, and README",
    "test": "npm run test:e2e -- --grep @claim:ics-restore-export",
    "sandbox": "fresh browser context at /demo/; download the cancelled sample event and inspect VCALENDAR, VERSION, VTIMEZONE, and event data"
  },
  {
    "id": "demo-private",
    "claim": "Sample data and demo controls stay separate from your archive",
    "where": "landing first screen, demo page, desktop demo banner, and README",
    "test": "npm run test:e2e -- --grep @claim:demo-private",
    "sandbox": "fresh browser context at /demo/ with production-license sentinels; exercise demo export, schedule panel, and reset, then compare storage namespaces"
  },
  {
    "id": "no-event-telemetry",
    "claim": "The app has no analytics or third-party calendar processing",
    "where": "landing privacy section, app lock screen, Privacy page, and README",
    "test": "npm run test:e2e -- --grep @claim:no-event-telemetry",
    "sandbox": "fresh browser context at /demo/; record the complete request log and assert every request is same-origin"
  },
  {
    "id": "encrypted-local-vault",
    "claim": "Calendar snapshots are encrypted before local storage",
    "where": "landing privacy section, vault screen, Privacy page, and README",
    "test": "npm run test:e2e -- --grep @claim:encrypted-local-vault",
    "sandbox": "fresh browser archive; import a known event and assert its text is absent from the IndexedDB envelope"
  },
  {
    "id": "encrypted-caldav-credentials",
    "claim": "Saved calendar connection details are encrypted before local storage",
    "where": "landing privacy section and scheduled connection dialog",
    "test": "npm run test:e2e -- --grep @claim:encrypted-caldav-credentials",
    "sandbox": "fresh browser archive with a fixture license; save known connection details and assert the IndexedDB envelope omits each value"
  },
  {
    "id": "passphrase-no-recovery",
    "claim": "The app cannot reset a lost vault passphrase",
    "where": "vault screen and Privacy page",
    "test": "npm run test:e2e -- --grep @claim:passphrase-no-recovery",
    "sandbox": "fresh browser archive; create a vault, lock it, attempt a different passphrase, and assert the vault stays locked"
  },
  {
    "id": "unchanged-refresh",
    "claim": "An unchanged calendar copy does not create another edition",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:unchanged-refresh",
    "sandbox": "fresh browser archive; import the same ICS file twice and assert one edition remains with an already-current notice"
  },
  {
    "id": "recurrence-timezones",
    "claim": "Preserves recurring overrides and timezone data in calendar copies and restore files",
    "where": "README",
    "test": "npm test -- -t @claim:recurrence-timezones",
    "sandbox": "fixture calendar with RECURRENCE-ID and VTIMEZONE; inspect parsed instances and generated restore content"
  },
  {
    "id": "calendar-connection",
    "claim": "Can record a calendar copy from a direct ICS or CalDAV calendar connection",
    "where": "README, Privacy page, and scheduled connection dialog",
    "test": "npm run test:e2e -- --grep @claim:calendar-connection",
    "sandbox": "fresh browser archive with a fixture license and mocked calendar endpoint; save the connection and record the returned calendar copy"
  },
  {
    "id": "native-caldav-transport",
    "claim": "The desktop transport uses direct ICS GET requests and CalDAV REPORT requests",
    "where": "README direct ICS and CalDAV connection description",
    "test": "npm run claim:native-caldav-transport",
    "sandbox": "self-provisioned native prerequisites and native transport unit fixture; assert GET for an ICS URL, REPORT for a CalDAV collection URL, and rejection of another URL scheme"
  },
  {
    "id": "scheduled-caldav",
    "claim": "An existing scheduling license enables scheduled calendar server copies while the app is open",
    "where": "landing license section, Terms page, desktop scheduling dialog, and README",
    "test": "npm run test:e2e -- --grep @claim:scheduled-caldav",
    "sandbox": "fresh browser archive with a fixture valid license; configure a 15-minute schedule, advance the browser clock one scheduler tick, and observe one calendar copy"
  },
  {
    "id": "license-price",
    "claim": "Manual calendar copies, review, archive backup, and restore files remain available without a license",
    "where": "landing first screen, license section, Terms page, desktop scheduling dialog, and README",
    "test": "npm run test:e2e -- --grep @claim:license-price",
    "sandbox": "fresh unlicensed browser archive; import changed calendar copies, review changes, export a restore file, export and restore the full encrypted archive, and assert no license gate or checkout action"
  },
  {
    "id": "demo-reset",
    "claim": "Reset demo restores the two shipped calendar copies without changing a real vault",
    "where": "demo banner, README, and .factory/demo.md",
    "test": "npm run test:e2e -- --grep @claim:demo-reset",
    "sandbox": "fresh browser context with a real-database sentinel; import a third demo copy, reset, and assert the seed and sentinel"
  },
  {
    "id": "demo-leave",
    "claim": "Leaving the sample discards changed sample data without changing a real vault",
    "where": "demo banner, README, and .factory/demo.md",
    "test": "npm run test:e2e -- --grep @claim:demo-leave",
    "sandbox": "fresh browser context with a real-vault sentinel; add a third sample copy, leave, re-enter, and assert the shipped two-copy seed"
  },
  {
    "id": "archive-roundtrip",
    "claim": "Exports and imports the full encrypted archive, including calendar copies and saved connection settings",
    "where": "desktop archive controls and README",
    "test": "npm run test:e2e -- --grep @claim:archive-roundtrip",
    "sandbox": "fresh browser vault; export an encrypted archive, replace local state, import it, and assert calendar copies and connection settings return"
  },
  {
    "id": "archive-wrong-passphrase",
    "claim": "An encrypted archive cannot be imported with the wrong passphrase",
    "where": "desktop archive import dialog",
    "test": "npm run test:e2e -- --grep @claim:archive-wrong-passphrase",
    "sandbox": "fresh browser vault and exported archive; enter a different passphrase and assert the open vault is unchanged"
  },
  {
    "id": "license-token-only",
    "claim": "License verification sends only the entered license token to Sociobot",
    "where": "desktop license dialog, Privacy page, and README",
    "test": "npm run test:e2e -- --grep @claim:license-token-only",
    "sandbox": "fresh browser app with mocked Sociobot verification; assert the request contains the token and no calendar data"
  },
  {
    "id": "free-accessibility-export",
    "claim": "Accessibility and calendar export features are available without a license",
    "where": "desktop app and README",
    "test": "npm run test:e2e -- --grep @claim:free-accessibility-export",
    "sandbox": "fresh unlicensed demo; use keyboard controls to select and export a changed event and run Axe"
  },
  {
    "id": "offline-local",
    "claim": "The open sample archive remains available without a network connection",
    "where": "landing first screen and desktop offline status",
    "test": "npm run test:e2e -- --grep @claim:offline-local",
    "sandbox": "fresh browser context at /demo/; go offline, select a cancelled event, and export its restore file"
  },
  {
    "id": "no-account-import",
    "claim": "Imports a calendar file without an account",
    "where": "README",
    "test": "npm run test:e2e -- --grep @claim:no-account-import",
    "sandbox": "fresh browser with no cookies or account state; create a vault and import a fixture calendar file"
  },
  {
    "id": "license-sales-paused",
    "claim": "New scheduling licenses are not currently for sale",
    "where": "landing first screen, license section, Privacy, Terms, and README",
    "test": "npm run test:e2e:static -- --grep @claim:license-sales-paused",
    "sandbox": "built static landing page; assert the sales pause and the absence of any checkout link"
  },
  {
    "id": "mit-license",
    "claim": "The software source is available under the MIT License",
    "where": "Terms page and README",
    "test": "npm test -- -t @claim:mit-license",
    "sandbox": "repository fixture; assert LICENSE contains the MIT grant and copyright notice"
  },
  {
    "id": "invalid-license-locks-scheduling",
    "claim": "A license that does not verify does not enable scheduling",
    "where": "Terms page and desktop license dialog",
    "test": "npm run test:e2e -- --grep @claim:invalid-license-locks-scheduling",
    "sandbox": "fresh browser app with mocked invalid Sociobot verification; assert scheduling remains unavailable"
  },
  {
    "id": "checksum-install",
    "claim": "The macOS and Linux command-line installer refuses a download with the wrong checksum",
    "where": "landing install section and README",
    "test": "npm test -- -t @claim:checksum-install",
    "sandbox": "temporary Linux install directory with fixture release manifest and payload; assert a matching payload installs and a mismatched payload is rejected before installation"
  },
  {
    "id": "unsigned-preview",
    "claim": "Preview installers are not code-signed",
    "where": "landing install notice and README",
    "test": "npm test -- -t @claim:unsigned-preview",
    "sandbox": "release workflow and Tauri bundle fixture; assert no macOS or Windows signing configuration is present"
  },
  {
    "id": "release-packaging",
    "claim": "Release packaging records its source commit and generates native package checksums and a platform manifest",
    "where": "README release workflow section",
    "test": "npm test -- -t @claim:release-packaging",
    "sandbox": "temporary native-package fixture; run the manifest generator and inspect every SHA256SUMS and latest.json platform entry"
  },
  {
    "id": "site-response-policy",
    "claim": "Static deployment includes security headers, asset caching, discovery files, and a styled 404 response",
    "where": "README release workflow section",
    "test": "npm test -- -t @claim:site-response-policy",
    "sandbox": "static deployment fixture; assert header policy, immutable asset route, robots.txt, sitemap.xml, and styled 404 override"
  },
  {
    "id": "release-trigger",
    "claim": "The release workflow runs for v tags and supports manual dispatch",
    "where": "README",
    "test": "npm test -- -t @claim:release-trigger",
    "sandbox": "release workflow fixture; assert the v tag trigger and workflow dispatch event"
  },
  {
    "id": "static-site-output",
    "claim": "Static deployment publishes the built site in dist/site",
    "where": "README",
    "test": "npm test -- -t @claim:static-site-output",
    "sandbox": "fresh build; run the static-site build and assert every public route is emitted under dist/site"
  },
  {
    "id": "release-downloads",
    "claim": "The landing page resolves a matching desktop download and sends phone visitors to desktop downloads",
    "where": "landing download action",
    "test": "npm run test:e2e -- --grep @claim:release-downloads",
    "sandbox": "desktop and Android/iPhone landing browser fixtures with mocked GitHub release metadata; assert desktop links target matching assets and phone links do not target a binary"
  },
  {
    "id": "delete-local-vault",
    "claim": "Delete local archive removes the vault from this device",
    "where": "Privacy page, desktop archive controls, and README",
    "test": "npm run test:e2e -- --grep @claim:delete-local-vault",
    "sandbox": "fresh browser archive; import a calendar copy, confirm Delete local archive, and assert the IndexedDB vault record is absent"
  },
  {
    "id": "runtime-requirements",
    "claim": "Node.js 22 or newer and stable Rust are the supported development toolchains",
    "where": "README Develop and release workflow",
    "test": "npm test -- -t @claim:runtime-requirements",
    "sandbox": "Node 22+ test process with repository engine and stable Rust declarations; invoke rustc and rustup, then cross-check the release workflow"
  },
  {
    "id": "native-prerequisite-setup",
    "claim": "On Ubuntu or Debian, setup installs missing native desktop libraries and a second run changes nothing",
    "where": "README Develop",
    "test": "npm test -- -t @claim:native-prerequisite-setup",
    "sandbox": "isolated apt and pkg-config fixture with every required module absent; run the real setup twice, assert all modules resolve, and assert no second package-manager call"
  }
]
